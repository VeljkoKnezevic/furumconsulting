---
// SolutionSection - Before/after transformation showing OTA independence
---

<section class="solution-section" id="solution">
  <div class="container">
    <div class="content-wrapper">
      <!-- Section header -->
      <div class="section-header">
        <h2>
          What If You Could Fill Rooms{" "}
          <span class="highlight">On Your Terms?</span>
        </h2>
        <p>
          We build your direct booking infrastructure, create compelling reasons to book direct,
          and drive qualified guests to your website instead of OTAs.
        </p>
      </div>

      <!-- Transformation visual -->
      <div class="transformation-card">
        <div class="transformation-grid">
          <!-- Before state -->
          <div class="state-block">
            <div class="state-circle state-before">
              <span>80%</span>
            </div>
            <h3>OTA Dependent</h3>
            <p>Trapped paying 15-30% commission on most bookings</p>
          </div>

          <!-- Arrow - Desktop -->
          <div class="arrow-desktop">
            <div class="arrow-line">
              <div class="arrow-dot"></div>
              <svg class="arrow-icon" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <line x1="5" y1="12" x2="19" y2="12"/>
                <polyline points="12 5 19 12 12 19"/>
              </svg>
            </div>
          </div>

          <!-- Arrow - Mobile -->
          <div class="arrow-mobile">
            <div class="arrow-line-vertical"></div>
          </div>

          <!-- After state -->
          <div class="state-block">
            <div class="state-circle state-after">
              <span>30%</span>
            </div>
            <h3>OTA Independent</h3>
            <p>Direct bookings drive your business forward</p>
          </div>
        </div>

        <!-- Key benefits -->
        <div class="benefits-grid">
          <div class="benefit">
            <div class="benefit-value">95%</div>
            <p>Revenue kept with direct bookings vs 70-85% through OTAs</p>
          </div>
          <div class="benefit">
            <div class="benefit-value">Owned</div>
            <p>Guest relationships that drive repeat bookings</p>
          </div>
          <div class="benefit">
            <div class="benefit-value">Control</div>
            <p>Your pricing, your brand, your future</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .solution-section {
    padding: 5rem 0;
    background: linear-gradient(
      135deg,
      hsl(var(--background)) 0%,
      hsl(var(--sage) / 0.1) 50%,
      hsl(var(--gold) / 0.05) 100%
    );
  }

  @media (min-width: 768px) {
    .solution-section {
      padding: 8rem 0;
    }
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
  }

  .content-wrapper {
    max-width: 64rem;
    margin: 0 auto;
  }

  /* Section header */
  .section-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .section-header h2 {
    font-family: var(--font-serif, Georgia, serif);
    font-size: clamp(1.875rem, 4vw, 3rem);
    font-weight: 700;
    color: hsl(var(--foreground));
    margin-bottom: 1.5rem;
    line-height: 1.2;
  }

  .section-header .highlight {
    color: hsl(var(--forest));
  }

  .section-header p {
    font-size: 1.125rem;
    color: hsl(var(--muted-foreground));
    max-width: 42rem;
    margin: 0 auto;
    line-height: 1.7;
  }

  /* Transformation card */
  .transformation-card {
    background: hsl(var(--card));
    border-radius: 1rem;
    border: 1px solid hsl(var(--border));
    box-shadow: 0 8px 32px hsl(var(--foreground) / 0.1);
    padding: 2rem;
  }

  @media (min-width: 768px) {
    .transformation-card {
      padding: 3rem;
    }
  }

  /* Transformation grid */
  .transformation-grid {
    display: grid;
    gap: 2rem;
    align-items: center;
  }

  @media (min-width: 768px) {
    .transformation-grid {
      grid-template-columns: 1fr auto 1fr;
    }
  }

  /* State blocks */
  .state-block {
    text-align: center;
  }

  .state-circle {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 5rem;
    height: 5rem;
    border-radius: 50%;
    margin-bottom: 1rem;
  }

  .state-circle span {
    font-family: var(--font-serif, Georgia, serif);
    font-size: 1.875rem;
    font-weight: 700;
  }

  .state-before {
    background: hsl(var(--terracotta) / 0.1);
  }

  .state-before span {
    color: hsl(var(--terracotta));
  }

  .state-after {
    background: hsl(var(--forest) / 0.1);
  }

  .state-after span {
    color: hsl(var(--forest));
  }

  .state-block h3 {
    font-size: 1.125rem;
    font-weight: 600;
    color: hsl(var(--foreground));
    margin-bottom: 0.5rem;
  }

  .state-block p {
    font-size: 0.875rem;
    color: hsl(var(--muted-foreground));
  }

  /* Arrow - Desktop */
  .arrow-desktop {
    display: none;
    align-items: center;
    justify-content: center;
    padding: 0 1rem;
  }

  @media (min-width: 768px) {
    .arrow-desktop {
      display: flex;
    }
  }

  .arrow-line {
    position: relative;
    display: flex;
    align-items: center;
    width: 100%;
    min-width: 80px;
    height: 4px;
    background: linear-gradient(
      to right,
      hsl(var(--terracotta)),
      hsl(var(--gold)),
      hsl(var(--forest))
    );
    border-radius: 2px;
  }

  .arrow-dot {
    position: absolute;
    right: 0;
    width: 12px;
    height: 12px;
    background: hsl(var(--forest));
    border-radius: 50%;
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.7; transform: scale(1.15); }
  }

  .arrow-icon {
    position: absolute;
    right: -8px;
    width: 24px;
    height: 24px;
    color: hsl(var(--forest));
  }

  /* Arrow - Mobile */
  .arrow-mobile {
    display: flex;
    justify-content: center;
  }

  @media (min-width: 768px) {
    .arrow-mobile {
      display: none;
    }
  }

  .arrow-line-vertical {
    width: 4px;
    height: 3rem;
    background: linear-gradient(
      to bottom,
      hsl(var(--terracotta)),
      hsl(var(--gold)),
      hsl(var(--forest))
    );
    border-radius: 2px;
  }

  /* Benefits grid */
  .benefits-grid {
    margin-top: 3rem;
    padding-top: 2rem;
    border-top: 1px solid hsl(var(--border));
    display: grid;
    gap: 1.5rem;
  }

  @media (min-width: 640px) {
    .benefits-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .benefit {
    text-align: center;
  }

  .benefit-value {
    font-family: var(--font-serif, Georgia, serif);
    font-size: 1.5rem;
    font-weight: 700;
    color: hsl(var(--gold));
    margin-bottom: 0.25rem;
  }

  .benefit p {
    font-size: 0.875rem;
    color: hsl(var(--muted-foreground));
    line-height: 1.5;
  }
</style>
